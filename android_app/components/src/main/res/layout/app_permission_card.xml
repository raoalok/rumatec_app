<?xml version="1.0" encoding="utf-8"?>
<!--
    MODERNIZED PERMISSION CARD - Material 3 Design
    
    Changes from original:
    - Replaced CardView with MaterialCardView for Material 3 support
    - Removed custom background drawable, using proper card styling
    - Increased corner radius to 12dp for modern look
    - Added proper elevation and ripple effect
    - Improved spacing using design tokens
    - Better icon sizing (48dp for accessibility)
    - Cleaner typography hierarchy
    - Added proper touch feedback with selectableItemBackground
    - Removed unnecessary nested LinearLayouts
    - Better alignment and padding consistency
-->
<com.google.android.material.card.MaterialCardView 
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/app_permission_card_container"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginStart="@dimen/spacing_sm"
    android:layout_marginEnd="@dimen/spacing_sm"
    android:layout_marginTop="@dimen/spacing_xs"
    android:layout_marginBottom="@dimen/spacing_xs"
    app:cardCornerRadius="12dp"
    app:cardElevation="@dimen/elevation_sm"
    app:cardBackgroundColor="@color/surface"
    app:strokeWidth="0dp"
    android:foreground="?attr/selectableItemBackground"
    android:clickable="true"
    android:focusable="true">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:minHeight="72dp"
        android:padding="@dimen/spacing_md">

        <!-- Permission Icon -->
        <com.google.android.material.imageview.ShapeableImageView
            android:id="@+id/item_icon"
            android:layout_width="@dimen/icon_size_xl"
            android:layout_height="@dimen/icon_size_xl"
            android:padding="@dimen/spacing_sm"
            android:scaleType="fitCenter"
            android:src="@drawable/user_data_svg"
            android:contentDescription="@null"
            app:tint="@color/primary"
            app:shapeAppearanceOverlay="@style/ShapeAppearance.App.CircleImageView"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            tools:src="@drawable/user_data_svg" />

        <!-- Permission Title -->
        <TextView
            android:id="@+id/item_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/spacing_md"
            android:layout_marginEnd="@dimen/spacing_sm"
            android:text="@string/permission_title_placeholder"
            android:textColor="@color/text_primary"
            android:textSize="@dimen/text_size_body1"
            android:fontFamily="sans-serif-medium"
            android:maxLines="2"
            android:ellipsize="end"
            app:layout_constraintStart_toEndOf="@id/item_icon"
            app:layout_constraintEnd_toStartOf="@id/permission_status_icon"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            tools:text="Camera Access" />

        <!-- Status Icon (Granted/Denied) -->
        <ImageView
            android:id="@+id/permission_status_icon"
            android:layout_width="@dimen/icon_size_md"
            android:layout_height="@dimen/icon_size_md"
            android:src="@drawable/ic_chevron_right"
            android:contentDescription="@string/permission_status"
            app:tint="@color/text_tertiary"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            tools:src="@drawable/ic_chevron_right" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</com.google.android.material.card.MaterialCardView>
